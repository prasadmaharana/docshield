# DocShield — PII Redactor for Scanned Financial Documents
# Install PyTorch first with the correct CUDA index URL:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# Then install the rest:
#   pip install -r requirements.txt

torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.4.0
paddleocr>=3.0.0
paddlepaddle>=3.0.0
pypdfium2>=4.0.0
Pillow>=9.0.0
numpy>=1.21.0
streamlit>=1.20.0
pandas>=1.5.0
pyyaml>=6.0
# Dataset download (training only)
datasets>=2.0.0
# Synthetic data generation (training only)
faker>=15.0.0
reportlab>=3.6.0
